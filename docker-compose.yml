services:
  web:
    # Для готового образа замените build: на:
    # image: ghcr.io/sarutobi/sopds-ng:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: final-prod
      args:
        BUILD_TARGET: prod
    image: ${DOCKER_IMAGE_TAG:-sopds-ng:prod}
    ports:
      - "8008:8008"
    volumes:
      - ./data:/app/data                      # конфиг и данные
      - ${SOPDS_BOOK_PATH:-./books}:/books:ro
    environment:
      DJANGO_SETTINGS_MODULE: "sopds.settings.base"
      SOPDS_ROOT: "/app"
      DATA_ROOT: "/app/data"
      PYTHONDONTWRITEBYTECODE: "true"
    depends_on:
      - db

  db:
    image: postgres:17
    restart: always
    shm_size: 128mb
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: ${SOPDS_DB_PASSWORD:-sopds}
      POSTGRES_DB: sopds
    volumes:
      - sopds-ng-db:/var/lib/postgresql/data/

volumes:
  sopds-ng-db:

# План оптимизации authors_services.py

## Текущие проблемы:
1. Дублирование кода в `search_authors` и `search_authors_with_counts`
2. Использование сырых SQL в `find_authors_by_template`
3. Отсутствие кэширования результатов поиска
4. Неоптимальные аннотации в запросах

## Требуемые изменения:

### 1. Устранить дублирование кода:
- Создать базовую функцию поиска авторов
- Использовать параметризованные запросы
- Вынести общую логику фильтрации

### 2. Заменить сырые SQL запросы:
- Использовать Django ORM вместо `Func` и `Substr`
- Создать кастомные менеджеры для Author
- Использовать аннотации Django

### 3. Добавить кэширование:
- Кэширование результатов поиска по шаблону
- Кэширование имен авторов по ID
- Кэширование счетчиков книг по авторам

### 4. Оптимизировать запросы:
- Использовать `select_related` для связанных данных
- Добавить индексы на `search_full_name`
- Оптимизировать подсчет книг

### 5. Улучшить типизацию:
- Заменить `TypedDict` на `NamedTuple` или dataclass
- Добавить более строгие проверки типов
- Использовать Literal для searchtype

### 6. Добавить пагинацию:
- Методы для пагинированного поиска авторов
- Поддержка разных стратегий сортировки
